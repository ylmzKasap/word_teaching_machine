{
  "name": "some-random-backend",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "sass": "cd client/src/styling && python compile_sass.py",
    "dev": "concurrently --kill-others \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^2.0.15"
  }
}
